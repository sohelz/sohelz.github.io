{
  "title": "RSP — Read SuperCollider Post Window",
  "date": "2026-04-16",
  "slug": "rsp-read-supercollider-post-window",
  "tags": ["braille-screen-reader-access-tools"],
  "body": "<p><a href=\"https://github.com/sohelz/rsp\">RSP (Read SuperCollider Post Window)</a> is a macOS accessibility tool that reads the SuperCollider Post Window aloud via VoiceOver with a single hotkey. Designed and built by Soheil Zarrinpour with the assistance of AI.</p>\n\n<h2>The Problem</h2>\n\n<p>When live-coding in SuperCollider, the Post Window constantly prints evaluation results, server messages, and debug output. For a blind or visually impaired user relying on VoiceOver, switching focus to the Post Window to read its content breaks the workflow and wastes valuable time — especially during a performance or a fast coding session.</p>\n\n<h2>How RSP Works</h2>\n\n<p>RSP is a macOS Automator application wrapping an AppleScript. When triggered by a hotkey, it does the following:</p>\n\n<p>1. Uses <strong>System Events</strong> to access the SuperCollider process and read the text content of the Post Window.<br>\n2. Parses the text into lines and finds the last non-empty line — the most recent output.<br>\n3. Sends that line to <strong>VoiceOver</strong>, which speaks it aloud immediately.</p>\n\n<p>The entire cycle happens in an instant. There is no window switching, no mouse interaction, and no disruption to the coding flow.</p>\n\n<h2>Tools Used</h2>\n\n<p><strong>macOS Automator</strong> — packages the script as a standalone <code>.app</code> that can be assigned a system-wide keyboard shortcut.<br>\n<strong>AppleScript</strong> — interfaces with SuperCollider's UI through the macOS accessibility API (System Events).<br>\n<strong>VoiceOver</strong> — Apple's built-in screen reader, used here to speak the extracted Post Window text.</p>\n\n<h2>Setup</h2>\n\n<p>Download or build <code>rsp.app</code> from the <a href=\"https://github.com/sohelz/rsp\">RSP GitHub page</a>, then assign it a hotkey through System Settings or a tool like Hammerspoon. Make sure VoiceOver is enabled and that both SuperCollider and the Automator app have Accessibility permissions in System Settings.</p>\n\n<p>With one keypress, you hear the latest Post Window output — no context switch, no wasted time.</p>"
}

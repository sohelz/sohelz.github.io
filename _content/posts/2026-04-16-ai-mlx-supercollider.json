{
  "title": "AI MLX — Local AI Code Translator for SuperCollider",
  "date": "2026-04-16",
  "slug": "ai-mlx-supercollider",
  "tags": ["code-lab"],
  "body": "<p><a href=\"https://github.com/sohelz/ai-mlx-supercollider\">AI MLX</a> is a SuperCollider tool that translates natural language shorthand into SuperCollider code using a local LLM running on Apple Silicon via MLX. No cloud API, no internet — everything runs on your machine. Designed and built by Soheil Zarrinpour with the assistance of AI.</p>\n\n<h2>What It Does</h2>\n\n<p>Instead of typing full SuperCollider syntax, you write shorthand like <code>sin440 0.1</code> or <code>lpf white 440</code>, and the tool returns clean SuperCollider code: <code>SinOsc.ar(440, 0, 0.1)</code> or <code>LPF.ar(WhiteNoise.ar(1), 440)</code>. It understands oscillators, noise generators, filters, envelopes, arrays, patterns, reverbs, delays, math operations, and more — a full dictionary of SuperCollider vocabulary.</p>\n\n<p>You can also use <code>~sc.(\"sin440 0.1\")</code> to translate and immediately play the result in one step.</p>\n\n<h2>How It Works</h2>\n\n<p>The tool uses three components working together:</p>\n\n<p><strong>MLX</strong> — Apple's machine learning framework optimized for Apple Silicon (M1/M2/M3/M4). It runs large language models locally using the GPU, with no cloud dependency.<br>\n<strong>mlx-lm</strong> — A Python command-line tool for generating text with MLX models. Install it with <code>pip3 install mlx-lm</code>.<br>\n<strong>Llama 3.1 8B (4-bit)</strong> — Meta's open language model, quantized to 4-bit for efficient local execution (~4 GB download on first use).</p>\n\n<p>From within SuperCollider, the script calls <code>mlx_lm.generate</code> as a shell process using SuperCollider's <code>Pipe</code> class. It sends a structured prompt containing a comprehensive dictionary of SuperCollider syntax, along with your shorthand input. The model returns the corresponding SuperCollider expression, which you can evaluate directly.</p>\n\n<h2>How to Use It</h2>\n\n<p>1. Install <code>mlx-lm</code>: <code>pip3 install mlx-lm</code><br>\n2. Open <code>AI MLX.scd</code> in SuperCollider and evaluate the function blocks.<br>\n3. If your Python path differs, update the path in the <code>Pipe.new(...)</code> call. Find yours with <code>which mlx_lm.generate</code> in the terminal.<br>\n4. Type shorthand and get code:</p>\n\n<pre><code>~ai.(\"sin440 0.1\")           // → SinOsc.ar(440, 0, 0.1)\n~ai.(\"lpf white 440\")        // → LPF.ar(WhiteNoise.ar(1), 440)\n~ai.(\"reverb saw 220 0.2\")   // → FreeVerb.ar(Saw.ar(220, 0.2))\n~ai.(\"close to 2 5 10 15 22\") // → Array.geom(5, 2, 2)\n~ai.(\"pbind sin rand freq 200 800 dur 0.25\")\n~sc.(\"sin440 0.1\")           // translates AND plays\n</code></pre>\n\n<h2>Requirements</h2>\n\n<p>macOS with Apple Silicon (M1 or later), Python 3, SuperCollider, and <code>mlx-lm</code>. The model downloads automatically on first run.</p>"
}
